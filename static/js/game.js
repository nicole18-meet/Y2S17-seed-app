var hour = getHours()	
var millsec = getMilliseconds()
var min = getMinutes()
while (1===1){
	if (hour === 10) && (millsec === 001){
    
	}
	if (hour === 10) && (min === 5){

	} 
}

- send email with python https://stackoverflow.com/questions/10147455/how-to-send-an-email-with-gmail-as-provider-using-python
- add button to page that trigger the email to e sent after
- add a link in the url that redirect to your website, 
  the view should be a post that modify a flag on the user 
-